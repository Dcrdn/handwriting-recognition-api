api for handwriting emoji recognition

create virtual env
python3 -m venv env

activate virtual env
source env/bin/activate

install dependencies
pip install -r requirements.txt

if dependencies updated
pip freeze > requirements.txt